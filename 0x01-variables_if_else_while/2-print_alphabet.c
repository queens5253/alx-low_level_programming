#include <stdio.h>
/**
 * main - program that prints alphabets
 * in lowercases
 * Return: 0 always
*/
int main(void)
{
	int ch;
	for (ch = 97; ch <= 122; ch++);
{
	putchar (ch);
}
	putchar(10);

return (0);
}



